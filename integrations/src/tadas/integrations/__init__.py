"""Integrations: the hosted services the platform depends on. Each is an
interface with a real client and a deterministic twin; the container wires
one of them at boot, like a backend of the infra root."""
