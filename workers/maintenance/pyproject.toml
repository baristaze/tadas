[project]
name = "tadas-maintenance"
version = "0.1.0"
description = "The Tadas maintenance worker: the claim loop, the sweep, and the Slack socket bridge."
requires-python = ">=3.14"
dependencies = [
    "tadas-om",
    "tadas-infra",
    "tadas-integrations",
    "pydantic-settings>=2.15",
]

[project.scripts]
tadas-maintenance = "tadas.workers.maintenance.entry:main"

[tool.uv.sources]
tadas-om = { workspace = true }
tadas-infra = { workspace = true }
tadas-integrations = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
# A subpackage of the shared root `tadas`, with no tadas/__init__.py (ADR 0001).
packages = ["src/tadas"]
